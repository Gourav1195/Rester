import React from 'react';
import {useList} from '@pankod/refine-core';
//useList is important refine hook. Which allows to get access of particular resource such as Property

const home = () => {
  return (
    <div>
        home
        </div>
  )
}

export default home